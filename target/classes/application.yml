spring:
  datasource:
    url: jdbc:mysql://datasource:3306/max_zvaryh_db_lab3?createDatabaseIfNotExist=true&useSSL=false
    username: Masyanya
    password: Max0993319943
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
