spring:
  datasource:
    url: jdbc:mysql://localhost/max_zvaryh_db_lab3?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: ***********
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8080